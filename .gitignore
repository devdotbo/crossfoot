# Build output.
target/

# Evidence bundles and the content-addressed RPC cache: reproducible from the
# network, never committed.
bundles/
cache/

# Rendered pages, regenerated from the bundles by `crossfoot render`.
site/

.env
*.DS_Store
.DS_Store

# Subgraph toolchain output (subgraph/): dependencies, codegen, build, and the
# matchstick binary cache. subgraph.yaml is generated but committed (04 R1).
subgraph/node_modules/
subgraph/build/
subgraph/generated/
subgraph/tests/.bin/
subgraph/tests/.latest.json
subgraph/subgraph-local.yaml
